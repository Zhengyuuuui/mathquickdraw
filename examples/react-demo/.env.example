# Copy to `.env.local` (gitignored) and point at your Worker.
# Leave VITE_API_BASE_URL unset to fall back to http://127.0.0.1:8787 —
# the board keeps working offline, it just can't persist.
VITE_API_BASE_URL=http://127.0.0.1:8787
VITE_APP_TOKEN=change-me
